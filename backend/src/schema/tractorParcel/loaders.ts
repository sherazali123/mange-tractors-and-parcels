import DataLoader from 'dataloader';
import { map, find } from 'lodash';
import { In } from 'typeorm';
import TractorParcel from './model';

export const createLoaders = (model: TractorParcel) => {
  return {};
};
